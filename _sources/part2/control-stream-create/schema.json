{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "allOf": [
    {
      "$ref": "bblocks://ogc.api.connected-systems.part2.control-stream"
    },
    {
      "properties": {
        "schema": {
          "description": "Schema describing the content of commands in this control stream. The exact syntax of the schema depends on the encoding format.",
          "$ref": "bblocks://ogc.api.connected-systems.part2.command-schema",
          "writeOnly": true
        }
      },
      "required": [
        "schema"
      ]
    }
  ]
}
